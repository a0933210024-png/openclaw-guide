---
export interface Props {
  title: string;
  description?: string;
  lang?: string;
  currentPath?: string;
  ogImage?: string;
}

const { title, description = "MiniBot（迷你兵）— 最簡單的 AI 助理入門教學，手把手教你在手機上使用 AI Bot", lang = "en", currentPath = "/", ogImage = "https://openclaw-guide-red.vercel.app/og-image.png" } = Astro.props;
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} — MiniBot</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description || 'MiniBot（迷你兵）— 最簡單的 AI 助理入門教學'} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:image" content={ogImage} />
    <meta property="og:site_name" content="MiniBot" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@MiniBot_AI" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description || 'MiniBot（迷你兵）— 最簡單的 AI 助理入門教學'} />
    <link rel="canonical" href={`https://openclaw-guide-red.vercel.app${currentPath || '/'}`} />
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "MiniBot 迷你兵",
        "url": "https://openclaw-guide-red.vercel.app",
        "description": "手把手教你在 Telegram、WhatsApp、Discord 裝上 AI 助理，不用寫程式",
        "potentialAction": {
          "@type": "SearchAction",
          "target": "https://openclaw-guide-red.vercel.app/skills",
          "query-input": "required name=search_term_string"
        }
      }
    </script>
  </head>
  <body>
    <nav class="nav">
      <div class="nav-inner">
        <a href={lang === "zh" ? "/zh/" : "/"} class="nav-logo">
          <span class="logo-icon">🦞</span>
          <span class="logo-text">MiniBot</span>
        </a>
        <div class="nav-links">
          {lang === "en" ? (
            <>
              <a href="/guide/install" class={currentPath.includes('/guide/install') ? 'active' : ''}>Install</a>
              <div class="nav-dropdown">
                <button class="nav-dropdown-btn" aria-expanded="false" aria-haspopup="true">
                  Connect ▾
                </button>
                <div class="nav-dropdown-menu">
                  <a href="/guide/telegram">🟦 Telegram</a>
                  <a href="/guide/whatsapp">🟩 WhatsApp</a>
                  <a href="/guide/discord">🟣 Discord</a>
                </div>
              </div>
              <a href="/use-cases" class={currentPath.includes('/use-cases') ? 'active' : ''}>Use Cases</a>
              <a href="/skills" class={currentPath.includes('/skills') ? 'active' : ''}>Skills</a>
              <a href="/models" class={currentPath.includes('/models') ? 'active' : ''}>Models</a>
              <a href="/starter-kit" class={currentPath.includes('/starter-kit') ? 'active' : ''}>
                <span class="nav-kit">Starter Kit</span> <span class="nav-price">$29</span>
              </a>
            </>
          ) : (
            <>
              <a href="/zh/guide/install" class={currentPath.includes('/guide/install') ? 'active' : ''}>安裝教學</a>
              <div class="nav-dropdown">
                <button class="nav-dropdown-btn" aria-expanded="false" aria-haspopup="true">
                  通訊交流 ▾
                </button>
                <div class="nav-dropdown-menu">
                  <a href="/zh/guide/telegram">🟦 Telegram</a>
                  <a href="/zh/guide/whatsapp">🟩 WhatsApp</a>
                  <a href="/zh/guide/discord">🟣 Discord</a>
                </div>
              </div>
              <a href="/zh/use-cases" class={currentPath.includes('/use-cases') ? 'active' : ''}>使用情境</a>
              <a href="/zh/skills" class={currentPath.includes('/skills') ? 'active' : ''}>Skills 技能包</a>
              <a href="/zh/models" class={currentPath.includes('/models') ? 'active' : ''}>模型指南</a>
              <a href="/zh/starter-kit" class={currentPath.includes('/starter-kit') ? 'active' : ''}>
                <span class="nav-kit">Starter Kit</span> <span class="nav-price">$29</span>
              </a>
            </>
          )}
          <div class="lang-switcher">
            {lang === "en" ? (
              <a href={currentPath.replace(/^\/(?!zh)/, '/zh/')} class="lang-btn lang-zh">中文</a>
            ) : (
              <a href={currentPath.replace(/^\/zh/, '')} class="lang-btn lang-en">EN</a>
            )}
          </div>
        </div>
        <button class="hamburger" id="hamburger" aria-label="Menu">
          <span></span><span></span><span></span>
        </button>
      </div>
    </nav>

    <main>
      <slot />
    </main>

    <footer class="footer">
      <div class="footer-inner">
        <div class="footer-brand">
          <span class="logo-icon">🦞</span>
          <span>MiniBot</span>
        </div>
        <p class="footer-desc">
          {lang === "en" 
            ? "MiniBot — An unofficial guide to OpenClaw, the self-hosted AI gateway."
            : "MiniBot（迷你兵）— OpenClaw 非官方教學，自架 AI 助理閘道器。"}
        </p>
        <div class="footer-links">
          <a href="https://docs.openclaw.ai" target="_blank" rel="noopener noreferrer">Official Docs</a>
          <a href="https://github.com" target="_blank" rel="noopener noreferrer">GitHub</a>
        </div>
        <p class="footer-copy">&copy; 2026 MiniBot. MIT License.</p>
      </div>
    </footer>

    <script>
      const hamburger = document.getElementById('hamburger');
      const navLinks = document.querySelector('.nav-links');

      // P1: Hamburger toggle
      if (hamburger && navLinks) {
        hamburger.addEventListener('click', () => {
          navLinks.classList.toggle('open');
          hamburger.classList.toggle('active');
        });
        // Close menu when clicking any nav link
        navLinks.querySelectorAll('a').forEach(link => {
          link.addEventListener('click', () => {
            navLinks.classList.remove('open');
            hamburger.classList.remove('active');
          });
        });
      }

      // Dropdown nav
      document.querySelectorAll('.nav-dropdown').forEach(dropdown => {
        const btn = dropdown.querySelector('.nav-dropdown-btn');
        btn?.addEventListener('click', (e) => {
          e.stopPropagation();
          dropdown.classList.toggle('open');
          btn.setAttribute('aria-expanded', dropdown.classList.contains('open') ? 'true' : 'false');
        });
        // P2: Close dropdown when clicking a link inside it
        dropdown.querySelectorAll('a').forEach(link => {
          link.addEventListener('click', () => {
            dropdown.classList.remove('open');
            dropdown.querySelector('.nav-dropdown-btn')?.setAttribute('aria-expanded', 'false');
          });
        });
      });
      // Close dropdown when clicking outside
      document.addEventListener('click', () => {
        document.querySelectorAll('.nav-dropdown.open').forEach(d => {
          d.classList.remove('open');
          d.querySelector('.nav-dropdown-btn')?.setAttribute('aria-expanded', 'false');
        });
      });

      // P9: Active state for nav links
      const currentPath = window.location.pathname;
      document.querySelectorAll('.nav-links a').forEach(link => {
        const href = link.getAttribute('href');
        if (href && href !== '/' && href !== '/zh/' && currentPath.startsWith(href)) {
          link.classList.add('active');
        } else if ((href === '/' || href === '/zh/') && (currentPath === href || currentPath === href.replace(/\/$/, ''))) {
          link.classList.add('active');
        }
      });
    </script>
  </body>
</html>

<style is:global>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg: #0a0a0a;
    --bg2: #111111;
    --bg3: #1a1a1a;
    --border: #2a2a2a;
    --text: #ffffff;
    --text2: #a0a0a0;
    --accent: #FF6B35;
    --accent2: #00D4FF;
    --accent-hover: #e05a28;
    --font: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    --mono: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace;
    --radius: 8px;
    --radius-lg: 16px;
    --shadow: 0 4px 24px rgba(0,0,0,0.4);
    --max-width: 1100px;
  }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font);
    font-size: 16px;
    line-height: 1.6;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  main { flex: 1; }

  a { color: var(--accent2); text-decoration: none; transition: opacity 0.2s; }
  a:hover { opacity: 0.8; }

  h1, h2, h3, h4 { line-height: 1.2; font-weight: 700; }
  h1 { font-size: clamp(2rem, 5vw, 3.5rem); }
  h2 { font-size: clamp(1.5rem, 3vw, 2.2rem); }
  h3 { font-size: 1.3rem; }

  p { color: var(--text2); }
  strong { color: var(--text); }

  code {
    font-family: var(--mono);
    background: var(--bg3);
    padding: 0.15em 0.4em;
    border-radius: 4px;
    font-size: 0.88em;
    color: var(--accent2);
  }

  pre {
    background: var(--bg3);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 1.25rem 1.5rem;
    overflow-x: auto;
    font-family: var(--mono);
    font-size: 0.875rem;
    line-height: 1.6;
    position: relative;
  }

  pre code {
    background: none;
    padding: 0;
    color: #e2e8f0;
    font-size: inherit;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.75rem;
    border-radius: var(--radius);
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    border: none;
    transition: all 0.2s;
    text-decoration: none;
  }

  .btn-primary {
    background: var(--accent);
    color: white;
  }
  .btn-primary:hover { background: var(--accent-hover); opacity: 1; transform: translateY(-1px); }

  .btn-outline {
    background: transparent;
    color: var(--text);
    border: 1px solid var(--border);
  }
  .btn-outline:hover { border-color: var(--accent2); color: var(--accent2); opacity: 1; }

  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .section { padding: 5rem 0; }
  .section-sm { padding: 3rem 0; }

  .badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: 999px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .badge-orange { background: rgba(255,107,53,0.15); color: var(--accent); border: 1px solid rgba(255,107,53,0.3); }
  .badge-cyan { background: rgba(0,212,255,0.1); color: var(--accent2); border: 1px solid rgba(0,212,255,0.2); }

  /* FAQ */
  .faq-item { border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; margin-bottom: 0.75rem; }
  .faq-q { padding: 1rem 1.25rem; font-weight: 600; cursor: pointer; display: flex; justify-content: space-between; align-items: center; }
  .faq-q:hover { background: var(--bg3); }
  .faq-a { padding: 0 1.25rem 1rem; display: none; }
  .faq-item.open .faq-a { display: block; }
  .faq-item.open .faq-arrow { transform: rotate(180deg); }
  .faq-arrow { transition: transform 0.2s; }

  /* Code hint */
  .code-hint {
    font-size: 0.78rem;
    color: var(--text2);
    margin-top: 0.25rem;
    margin-bottom: 0.75rem;
  }

  /* 手機版全局修復 */
  @media (max-width: 768px) {
    .container {
      padding-left: 1rem;
      padding-right: 1rem;
    }

    pre, code {
      font-size: 0.78rem;
      overflow-x: auto;
      white-space: pre;
      word-wrap: normal;
    }

    pre {
      padding: 0.75rem 1rem;
    }

    table {
      display: block;
      overflow-x: auto;
      white-space: nowrap;
    }

    .content-grid {
      grid-template-columns: 1fr !important;
    }
    .toc { display: none; }

    .requirements-grid,
    .why-grid,
    .provider-cards,
    .option-cards {
      grid-template-columns: 1fr !important;
    }

    .nav-buttons {
      flex-direction: column !important;
      gap: 1rem !important;
    }

    .page-hero {
      padding: 2rem 0 1.5rem !important;
    }
    .page-hero h1 {
      font-size: 1.75rem !important;
    }
    .page-subtitle {
      font-size: 1rem !important;
    }
  }
</style>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(10,10,10,0.9);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }

  .nav-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--text);
    flex-shrink: 0;
  }
  .nav-logo:hover { opacity: 0.9; }
  .logo-icon { font-size: 1.4rem; }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    flex-wrap: nowrap;
  }

  .nav-links a {
    color: var(--text2);
    font-size: 0.9rem;
    padding: 0.4rem 0.75rem;
    border-radius: 6px;
    transition: all 0.2s;
  }
  .nav-links a:hover, .nav-links a.active { color: var(--accent); background: var(--bg3); opacity: 1; }

  .nav-kit { display: inline; }
  .nav-price {
    display: inline-block;
    background: var(--accent);
    color: white;
    font-size: 0.7rem;
    font-weight: 700;
    padding: 0.1rem 0.4rem;
    border-radius: 999px;
    margin-left: 0.3rem;
    vertical-align: middle;
  }

  .lang-switcher { margin-left: 0.5rem; }
  .lang-btn {
    padding: 0.35rem 0.85rem !important;
    border-radius: 999px !important;
    font-weight: 600 !important;
    font-size: 0.8rem !important;
  }
  .lang-en { background: rgba(0,212,255,0.1) !important; color: var(--accent2) !important; border: 1px solid rgba(0,212,255,0.3); }
  .lang-zh { background: rgba(255,107,53,0.1) !important; color: var(--accent) !important; border: 1px solid rgba(255,107,53,0.3); }

  /* Dropdown nav */
  .nav-dropdown { position: relative; }
  .nav-dropdown-btn {
    background: none;
    border: none;
    color: var(--text2);
    font-size: 0.9rem;
    padding: 0.4rem 0.75rem;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.2s;
    font-family: var(--font);
  }
  .nav-dropdown-btn:hover { color: var(--text); background: var(--bg3); }
  .nav-dropdown-btn.active { color: var(--text); background: var(--bg3); }
  .nav-dropdown-menu {
    display: none;
    position: absolute;
    top: calc(100% + 0.5rem);
    left: 0;
    min-width: 160px;
    background: var(--bg2);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    z-index: 200;
    overflow: hidden;
  }
  .nav-dropdown-menu a {
    display: block;
    padding: 0.6rem 1rem;
    color: var(--text2);
    font-size: 0.9rem;
    transition: all 0.15s;
  }
  .nav-dropdown-menu a:hover { background: var(--bg3); color: var(--text); opacity: 1; }
  .nav-dropdown.open .nav-dropdown-menu { display: block; }
  .nav-dropdown.open .nav-dropdown-btn { color: var(--text); background: var(--bg3); }

  .hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
  }
  .hamburger span {
    display: block;
    width: 22px;
    height: 2px;
    background: var(--text);
    border-radius: 2px;
    transition: all 0.3s;
  }

  .footer {
    background: var(--bg2);
    border-top: 1px solid var(--border);
    padding: 3rem 0 2rem;
    margin-top: auto;
  }

  .footer-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
    text-align: center;
  }

  .footer-brand {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-weight: 700;
    font-size: 1.1rem;
    margin-bottom: 0.75rem;
  }

  .footer-desc { margin-bottom: 1.25rem; font-size: 0.9rem; }

  .footer-links {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    font-size: 0.9rem;
  }

  .footer-copy { font-size: 0.8rem; color: #555; }

  @media (max-width: 768px) {
    .hamburger { display: flex; }
    .nav-links {
      display: none;
      position: absolute;
      top: 64px;
      left: 0;
      right: 0;
      background: var(--bg2);
      border-bottom: 1px solid var(--border);
      flex-direction: column;
      padding: 1rem 1.5rem;
      gap: 0.5rem;
      align-items: flex-start;
    }
    .nav-links.open { display: flex; }
    .lang-switcher { margin-left: 0; margin-top: 0.5rem; }

    /* Mobile dropdown: always expanded in mobile menu */
    .nav-dropdown { width: 100%; }
    .nav-dropdown-btn { padding-left: 0; }
    .nav-dropdown-menu {
      display: block;
      position: static;
      box-shadow: none;
      border: none;
      background: transparent;
      padding-left: 0.75rem;
      min-width: auto;
      border-left: 2px solid var(--border);
      margin: 0.25rem 0;
    }
    .nav-dropdown-menu a { padding: 0.4rem 0.75rem; }
  }
</style>
