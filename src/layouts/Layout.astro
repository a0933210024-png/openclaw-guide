---
export interface Props {
  title: string;
  description?: string;
  lang?: string;
  currentPath?: string;
}

const { title, description = "OpenClaw Guide — Your AI Assistant, Actually Working", lang = "en", currentPath = "/" } = Astro.props;
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title} | OpenClaw Guide</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
  </head>
  <body>
    <nav class="nav">
      <div class="nav-inner">
        <a href={lang === "zh" ? "/zh/" : "/"} class="nav-logo">
          <span class="logo-icon">🦞</span>
          <span class="logo-text">OpenClaw Guide</span>
        </a>
        <div class="nav-links">
          {lang === "en" ? (
            <>
              <a href="/guide/install" class={currentPath.includes('/guide/install') ? 'active' : ''}>Install</a>
              <a href="/guide/telegram" class={currentPath.includes('/guide/telegram') ? 'active' : ''}>Telegram</a>
              <a href="/guide/first-persona" class={currentPath.includes('/guide/first-persona') ? 'active' : ''}>Persona</a>
              <a href="/guide/blackpink-case" class={currentPath.includes('/guide/blackpink') ? 'active' : ''}>Case Study</a>
            </>
          ) : (
            <>
              <a href="/zh/guide/install" class={currentPath.includes('/guide/install') ? 'active' : ''}>安裝教學</a>
              <a href="/zh/guide/telegram" class={currentPath.includes('/guide/telegram') ? 'active' : ''}>Telegram</a>
              <a href="/zh/guide/first-persona" class={currentPath.includes('/guide/first-persona') ? 'active' : ''}>AI Persona</a>
              <a href="/zh/guide/blackpink-case" class={currentPath.includes('/guide/blackpink') ? 'active' : ''}>實際案例</a>
            </>
          )}
          <div class="lang-switcher">
            {lang === "en" ? (
              <a href={currentPath.replace(/^\/(?!zh)/, '/zh/')} class="lang-btn lang-zh">中文</a>
            ) : (
              <a href={currentPath.replace(/^\/zh/, '')} class="lang-btn lang-en">EN</a>
            )}
          </div>
        </div>
        <button class="hamburger" id="hamburger" aria-label="Menu">
          <span></span><span></span><span></span>
        </button>
      </div>
    </nav>

    <main>
      <slot />
    </main>

    <footer class="footer">
      <div class="footer-inner">
        <div class="footer-brand">
          <span class="logo-icon">🦞</span>
          <span>OpenClaw Guide</span>
        </div>
        <p class="footer-desc">
          {lang === "en" 
            ? "An unofficial guide to OpenClaw — the self-hosted AI gateway."
            : "OpenClaw 非官方使用指南 — 自架 AI 助理閘道器。"}
        </p>
        <div class="footer-links">
          <a href="https://docs.openclaw.ai" target="_blank" rel="noopener">Official Docs</a>
          <a href="https://github.com" target="_blank" rel="noopener">GitHub</a>
        </div>
        <p class="footer-copy">&copy; 2026 OpenClaw Guide. MIT License.</p>
      </div>
    </footer>

    <script>
      const hamburger = document.getElementById('hamburger');
      const navLinks = document.querySelector('.nav-links');
      hamburger?.addEventListener('click', () => {
        navLinks?.classList.toggle('open');
        hamburger.classList.toggle('active');
      });
    </script>
  </body>
</html>

<style is:global>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --bg: #0a0a0a;
    --bg2: #111111;
    --bg3: #1a1a1a;
    --border: #2a2a2a;
    --text: #ffffff;
    --text2: #a0a0a0;
    --accent: #FF6B35;
    --accent2: #00D4FF;
    --accent-hover: #e05a28;
    --font: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    --mono: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace;
    --radius: 8px;
    --radius-lg: 16px;
    --shadow: 0 4px 24px rgba(0,0,0,0.4);
    --max-width: 1100px;
  }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font);
    font-size: 16px;
    line-height: 1.6;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  main { flex: 1; }

  a { color: var(--accent2); text-decoration: none; transition: opacity 0.2s; }
  a:hover { opacity: 0.8; }

  h1, h2, h3, h4 { line-height: 1.2; font-weight: 700; }
  h1 { font-size: clamp(2rem, 5vw, 3.5rem); }
  h2 { font-size: clamp(1.5rem, 3vw, 2.2rem); }
  h3 { font-size: 1.3rem; }

  p { color: var(--text2); }
  strong { color: var(--text); }

  code {
    font-family: var(--mono);
    background: var(--bg3);
    padding: 0.15em 0.4em;
    border-radius: 4px;
    font-size: 0.88em;
    color: var(--accent2);
  }

  pre {
    background: var(--bg3);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 1.25rem 1.5rem;
    overflow-x: auto;
    font-family: var(--mono);
    font-size: 0.875rem;
    line-height: 1.6;
    position: relative;
  }

  pre code {
    background: none;
    padding: 0;
    color: #e2e8f0;
    font-size: inherit;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.75rem;
    border-radius: var(--radius);
    font-weight: 600;
    font-size: 1rem;
    cursor: pointer;
    border: none;
    transition: all 0.2s;
    text-decoration: none;
  }

  .btn-primary {
    background: var(--accent);
    color: white;
  }
  .btn-primary:hover { background: var(--accent-hover); opacity: 1; transform: translateY(-1px); }

  .btn-outline {
    background: transparent;
    color: var(--text);
    border: 1px solid var(--border);
  }
  .btn-outline:hover { border-color: var(--accent2); color: var(--accent2); opacity: 1; }

  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .section { padding: 5rem 0; }
  .section-sm { padding: 3rem 0; }

  .badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    border-radius: 999px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .badge-orange { background: rgba(255,107,53,0.15); color: var(--accent); border: 1px solid rgba(255,107,53,0.3); }
  .badge-cyan { background: rgba(0,212,255,0.1); color: var(--accent2); border: 1px solid rgba(0,212,255,0.2); }

  /* FAQ */
  .faq-item { border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; margin-bottom: 0.75rem; }
  .faq-q { padding: 1rem 1.25rem; font-weight: 600; cursor: pointer; display: flex; justify-content: space-between; align-items: center; }
  .faq-q:hover { background: var(--bg3); }
  .faq-a { padding: 0 1.25rem 1rem; display: none; }
  .faq-item.open .faq-a { display: block; }
  .faq-item.open .faq-arrow { transform: rotate(180deg); }
  .faq-arrow { transition: transform 0.2s; }
</style>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(10,10,10,0.9);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }

  .nav-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--text);
    flex-shrink: 0;
  }
  .nav-logo:hover { opacity: 0.9; }
  .logo-icon { font-size: 1.4rem; }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    flex-wrap: nowrap;
  }

  .nav-links a {
    color: var(--text2);
    font-size: 0.9rem;
    padding: 0.4rem 0.75rem;
    border-radius: 6px;
    transition: all 0.2s;
  }
  .nav-links a:hover, .nav-links a.active { color: var(--text); background: var(--bg3); opacity: 1; }

  .lang-switcher { margin-left: 0.5rem; }
  .lang-btn {
    padding: 0.35rem 0.85rem !important;
    border-radius: 999px !important;
    font-weight: 600 !important;
    font-size: 0.8rem !important;
  }
  .lang-en { background: rgba(0,212,255,0.1) !important; color: var(--accent2) !important; border: 1px solid rgba(0,212,255,0.3); }
  .lang-zh { background: rgba(255,107,53,0.1) !important; color: var(--accent) !important; border: 1px solid rgba(255,107,53,0.3); }

  .hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
  }
  .hamburger span {
    display: block;
    width: 22px;
    height: 2px;
    background: var(--text);
    border-radius: 2px;
    transition: all 0.3s;
  }

  .footer {
    background: var(--bg2);
    border-top: 1px solid var(--border);
    padding: 3rem 0 2rem;
    margin-top: auto;
  }

  .footer-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
    text-align: center;
  }

  .footer-brand {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-weight: 700;
    font-size: 1.1rem;
    margin-bottom: 0.75rem;
  }

  .footer-desc { margin-bottom: 1.25rem; font-size: 0.9rem; }

  .footer-links {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    font-size: 0.9rem;
  }

  .footer-copy { font-size: 0.8rem; color: #555; }

  @media (max-width: 768px) {
    .hamburger { display: flex; }
    .nav-links {
      display: none;
      position: absolute;
      top: 64px;
      left: 0;
      right: 0;
      background: var(--bg2);
      border-bottom: 1px solid var(--border);
      flex-direction: column;
      padding: 1rem 1.5rem;
      gap: 0.5rem;
      align-items: flex-start;
    }
    .nav-links.open { display: flex; }
    .lang-switcher { margin-left: 0; margin-top: 0.5rem; }
  }
</style>
